import React from 'react';
import ReactDOM from 'react-dom';
import '../index.css';
import EditAccountMappingPage from './component/edit-account-mapping-page';

ReactDOM.render(
  <React.StrictMode>
    <EditAccountMappingPage />
  </React.StrictMode>,
  document.getElementById('root'),
);
