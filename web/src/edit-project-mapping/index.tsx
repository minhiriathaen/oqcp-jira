import React from 'react';
import ReactDOM from 'react-dom';
import '../index.css';
import EditProjectMappingPage from './component/edit-project-mapping-page';

ReactDOM.render(
  <React.StrictMode>
    <EditProjectMappingPage />
  </React.StrictMode>,
  document.getElementById('root'),
);
